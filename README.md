# img2ascii
ASCII Art generator for old impact printers

img2ascii.py - Python program to convert images into plain text files for old impact printers.

Optional files (relevant only if you want to print to ASCII only printers from MacOS)

textonly.ppd - PPD file for CUPS to enable text-only printers
textonly     - Filter for CUPS to process text-only documents
